// Delay the page load by 1000 milliseconds (1 second)
setTimeout(function () {
    // Get the page wrapper element
    var pageWrapper = document.getElementById("page-wrapper");
    // Set the opacity to 1 to reveal the content
    pageWrapper.style.opacity = "1";
}, 1000);
